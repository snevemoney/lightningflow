# Required. The admin UI fails closed if this is unset or empty.
# Generate your own value. Do not commit a real token.
ADMIN_ACCESS_TOKEN=

# Used only when constructing LightningService (not the mock node page).
# LNBITS_URL=
# LNBITS_ADMIN_KEY=
