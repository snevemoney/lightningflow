# Hardening 20-point audit (dry-run)

| Field | Value |
| --- | --- |
| Date | 2026-08-27 |
| Target | `main` @ `c174ea3` |
| Mode | Dry-run only. No application behavior changes. No refactors. No merge. |
| Scope | Tracked source on `main`. Skipped `node_modules`, lockfiles, and build output. |
| Repo state | GH stub. README marks this tree SUPERSEDED. Present code: `apps/admin` (4 TSX files) and `packages/lightning-core` (2 TS files). No `app/api`, no DB/Prisma/SQL, no `.env*`, no `middleware.ts`. |

Audited files:

- `apps/admin/app/agents/page.tsx`
- `apps/admin/app/layout.tsx`
- `apps/admin/app/node/page.tsx`
- `apps/admin/components/sidebar.tsx`
- `apps/admin/package.json`
- `packages/lightning-core/src/index.ts`
- `packages/lightning-core/src/lnbits.ts`
- `packages/lightning-core/package.json`
- `packages/lightning-core/tsconfig.json`
- `package.json`
- `turbo.json`
- `README.md`
- `.gitignore`
- `.gitattributes`

---

## 1. Duplicate utilities

**Status:** none found

No shared helper is implemented more than once. There is no `lib/utils` (or other util module) in tree; `cn` is imported but not defined here. `LightningConfig` and `WalletConfig` both have an `adminKey` field, but they are distinct types, not duplicate utilities.

none found

---

## 2. Secrets in config

**Status:** none found

No committed secret values (API keys, tokens, passwords, private keys, connection strings) in config or source. Env *references* exist at `apps/admin/app/node/page.tsx:16-17` (`LNBITS_URL`, `LNBITS_ADMIN_KEY`) but no values are in repo. Type/parameter names `adminKey` / `invoiceKey` are not secrets.

none found

---

## 3. Functions over 400 lines

**Status:** none found

Largest function/component bodies (line counts of the function, not the file):

| Symbol | File | Lines | Count |
| --- | --- | --- | --- |
| `AgentsPage` | `apps/admin/app/agents/page.tsx` | 61–152 | 92 |
| `NodePage` | `apps/admin/app/node/page.tsx` | 31–94 | 64 |
| `Sidebar` | `apps/admin/components/sidebar.tsx` | 66–101 | 36 |
| `RootLayout` | `apps/admin/app/layout.tsx` | 14–38 | 25 |
| `LightningService.createWallet` | `packages/lightning-core/src/lnbits.ts` | 20–36 | 17 |
| `getNodeStats` | `apps/admin/app/node/page.tsx` | 13–29 | 17 |

none found

---

## 4. Components over 200 lines

**Status:** none found

| Component | File | File lines | Component lines |
| --- | --- | --- | --- |
| `AgentsPage` | `apps/admin/app/agents/page.tsx` | 151 | 61–152 (92) |
| `Sidebar` | `apps/admin/components/sidebar.tsx` | 100 | 66–101 (36) |
| `NodePage` | `apps/admin/app/node/page.tsx` | 93 | 31–94 (64) |
| `RootLayout` | `apps/admin/app/layout.tsx` | 37 | 14–38 (25) |

none found

---

## 5. Dead code

**Status:** proposed

| # | File | Line | Finding |
| --- | --- | --- | --- |
| 1 | `apps/admin/app/node/page.tsx` | 15–18 | `LightningService` is constructed into `lightning` and never read. Mock object is returned instead. |
| 2 | `packages/lightning-core/src/lnbits.ts` | 38 | `createInvoice(walletId, …)` — `walletId` is never passed to `this.client.createInvoice`. |
| 3 | `packages/lightning-core/src/lnbits.ts` | 82 | `payInvoice(walletId, …)` — `walletId` is never passed to `this.client.payInvoice`. |
| 4 | `apps/admin/components/sidebar.tsx` | 20, 26, 32, 38, 44, 50, 56, 62 | `description` on every `navigation` item is never rendered. |
| 5 | `packages/lightning-core/package.json` | 17 | `grpc` is declared and never imported. |
| 6 | `packages/lightning-core/package.json` | 18 | `protobufjs` is declared and never imported. |

Not counted as dead code (missing / unused-public-API, not unreachable source):

- Unused-in-repo type exports `LightningConfig`, `Invoice`, `PaymentStatus`, `WalletBalance` in `packages/lightning-core/src/index.ts` (package public surface).
- Broken imports to files that are not in tree: `@/components/ui/card`, `@/components/ui/badge`, `@/components/theme-provider`, `@/lib/utils`, `./globals.css`, workspace `@lightning-platform/ui`.
- Sidebar hrefs `/clients`, `/wallets`, `/lightning`, `/analytics`, `/security`, `/settings` have no matching pages.

---

## 6. Silent / empty catch

**Status:** none found

All `catch` blocks rethrow:

| File | Line | Behavior |
| --- | --- | --- |
| `packages/lightning-core/src/lnbits.ts` | 33–35 | `throw new Error(\`Failed to create wallet: ${error.message}\`)` |
| `packages/lightning-core/src/lnbits.ts` | 52–54 | rethrow create-invoice failure |
| `packages/lightning-core/src/lnbits.ts` | 65–67 | rethrow check-invoice failure |
| `packages/lightning-core/src/lnbits.ts` | 77–79 | rethrow balance failure |
| `packages/lightning-core/src/lnbits.ts` | 93–95 | rethrow pay-invoice failure |

none found

---

## 7. UI API calls missing loading / error

**Status:** proposed

| # | File | Line | Finding |
| --- | --- | --- | --- |
| 1 | `apps/admin/app/node/page.tsx` | 13–28, 32 | `NodePage` awaits `getNodeStats()` with no `try/catch`. `getNodeStats` constructs `LightningService` with asserted env vars and does not handle constructor or later client failure. No `apps/admin/app/loading.tsx`, `error.tsx`, `node/loading.tsx`, or `node/error.tsx`. No in-page loading or error UI. |

`apps/admin/app/agents/page.tsx` uses an in-module mock array only; no API call.

---

## 8. DB queries in route handlers

**Status:** none found

No Prisma/SQL/ORM usage. No `app/api` route handlers. `NodePage` / `getNodeStats` are a Server Component and a local helper, not a route handler, and they do not query a database.

none found

---

## 9. Sync I/O in request handlers

**Status:** none found

No `readFileSync`, `writeFileSync`, `execSync`, or other sync filesystem/child-process I/O. No request handlers exist.

none found

---

## 10. List endpoints without pagination

**Status:** none found

No HTTP list endpoints. `agents` in `apps/admin/app/agents/page.tsx:13–46` is a hardcoded page-local array, not an API.

none found

---

## 11. Inconsistent API shapes

**Status:** proposed

No HTTP API. Findings are on the only service surface, `LightningService`:

| # | File | Line | Finding |
| --- | --- | --- | --- |
| 1 | `packages/lightning-core/src/lnbits.ts` | 20 vs 38, 57, 70, 82 | `createWallet` is `Promise<WalletConfig>`; `createInvoice`, `checkInvoice`, `getWalletBalance`, and `payInvoice` have no declared return type. |
| 2 | `packages/lightning-core/src/lnbits.ts` | 46–51, 60–64, 73–76, 88–92 vs `packages/lightning-core/src/index.ts` 9–25 | Method results match `Invoice` / `PaymentStatus` / `WalletBalance` structurally but do not use those exported types. |
| 3 | `packages/lightning-core/src/lnbits.ts` | 61 vs 89 | `checkInvoice` returns `paid: status.paid`; `payInvoice` hardcodes `paid: true`. |

---

## 12. Floats for money

**Status:** proposed

JS/TS `number` is IEEE-754 float. Monetary / satoshi fields use it:

| # | File | Line | Finding |
| --- | --- | --- | --- |
| 1 | `packages/lightning-core/src/index.ts` | 12 | `Invoice.amount: number` |
| 2 | `packages/lightning-core/src/index.ts` | 23 | `WalletBalance.balance: number` |
| 3 | `packages/lightning-core/src/lnbits.ts` | 38 | `createInvoice(…, amount: number, …)` |
| 4 | `packages/lightning-core/src/lnbits.ts` | 49 | invoice result copies `amount` as `number` |
| 5 | `packages/lightning-core/src/lnbits.ts` | 74 | `balance: balance.balance` typed/returned as `number` |
| 6 | `apps/admin/app/node/page.tsx` | 8 | `NodeStats.totalCapacity: number` (sats) |
| 7 | `apps/admin/app/node/page.tsx` | 25 | mock `totalCapacity: 5000000` |

No `bigint`, integer-string, or decimal type is used for amounts.

---

## 13. Dates as plain strings not ISO 8601

**Status:** proposed

| # | File | Line | Finding |
| --- | --- | --- | --- |
| 1 | `apps/admin/app/agents/page.tsx` | 9 | `Agent.lastActive: string` — not `Date` and not ISO 8601. |
| 2 | `apps/admin/app/agents/page.tsx` | 19 | `'2 minutes ago'` |
| 3 | `apps/admin/app/agents/page.tsx` | 26 | `'5 minutes ago'` |
| 4 | `apps/admin/app/agents/page.tsx` | 33 | `'1 hour ago'` |
| 5 | `apps/admin/app/agents/page.tsx` | 40 | `'3 hours ago'` |

No other date fields in source.

---

## 14. External calls without retry / backoff

**Status:** proposed

Every `LNBitsClient` network call is a single attempt. No retry, backoff, timeout wrapper, or circuit breaker.

| # | File | Line | Call |
| --- | --- | --- | --- |
| 1 | `packages/lightning-core/src/lnbits.ts` | 22–25 | `this.client.createWallet` |
| 2 | `packages/lightning-core/src/lnbits.ts` | 40–44 | `this.client.createInvoice` |
| 3 | `packages/lightning-core/src/lnbits.ts` | 59 | `this.client.checkInvoice` |
| 4 | `packages/lightning-core/src/lnbits.ts` | 72 | `this.client.getWalletDetails` |
| 5 | `packages/lightning-core/src/lnbits.ts` | 84–87 | `this.client.payInvoice` |

---

## 15. Stale comments

**Status:** proposed

| # | File | Line | Finding |
| --- | --- | --- | --- |
| 1 | `packages/lightning-core/src/index.ts` | 3 | Comment says "Re-export common types"; the types are defined in this file, not re-exported. |
| 2 | `apps/admin/app/node/page.tsx` | 14 | Comment says LightningService implementation will replace this path; the constructor already runs on 15–18, then mock data is returned. |
| 3 | `README.md` | 20–29 | Documents `/apps/web`, `/apps/partner-portal`, `/packages/auth`, `/packages/agents`, `/packages/ui`, `/scripts` — none exist on `main`. |
| 4 | `README.md` | 40–41 | Instructs `cp .env.example .env.local`; `.env.example` is not in the tree. |

`apps/admin/app/node/page.tsx:20` (`// For now return mock data`) matches current behavior; not counted.

---

## 16. Unvalidated input

**Status:** proposed

No schema/validator library (zod, yup, etc.) and no checks on public method arguments or form fields.

| # | File | Line | Finding |
| --- | --- | --- | --- |
| 1 | `packages/lightning-core/src/lnbits.ts` | 20 | `createWallet(name)` — no non-empty / length check. |
| 2 | `packages/lightning-core/src/lnbits.ts` | 38 | `createInvoice(walletId, amount, memo)` — no id format, no integer/`> 0` amount, no memo bound. |
| 3 | `packages/lightning-core/src/lnbits.ts` | 57 | `checkInvoice(paymentHash)` — no hash format check. |
| 4 | `packages/lightning-core/src/lnbits.ts` | 70 | `getWalletBalance(walletId)` — no id check. |
| 5 | `packages/lightning-core/src/lnbits.ts` | 82 | `payInvoice(walletId, bolt11)` — no wallet id or `ln…` bolt11 check. |
| 6 | `apps/admin/app/node/page.tsx` | 16–17 | `process.env.LNBITS_URL!` and `process.env.LNBITS_ADMIN_KEY!` asserted, never validated. |
| 7 | `apps/admin/app/agents/page.tsx` | 129–133 | Knowledge Base Path `<input>` has no validation (and no submit handler). |
| 8 | `apps/admin/app/agents/page.tsx` | 139–143 | Embedding Model `<select>` has no validation (and no submit handler). |

---

## 17. API routes missing auth

**Status:** none found

No `app/api/**` (or other) route handlers exist, so none are missing auth.

Observation, not counted: admin UI pages (`apps/admin/app/layout.tsx:14`, `apps/admin/app/node/page.tsx:31`, `apps/admin/app/agents/page.tsx:61`) have no session, middleware, or auth check. There is no `middleware.ts`.

none found

---

## 18. Missing indexes

**Status:** none found

No database schema, migrations, or query layer.

none found

---

## 19. N+1 queries

**Status:** none found

No database access and no per-item query loops. `agents.map` in `apps/admin/app/agents/page.tsx:72` iterates in-memory mocks only.

none found

---

## 20. SDKs initialized in more than one place

**Status:** none found

| SDK / client | Init site | Other sites |
| --- | --- | --- |
| `LNBitsClient` | `packages/lightning-core/src/lnbits.ts:14` | none |
| `LightningService` | `apps/admin/app/node/page.tsx:15` | none |

none found

---

## Summary

| check | status | count |
| --- | --- | --- |
| 1 duplicate utilities | none found | 0 |
| 2 secrets in config | none found | 0 |
| 3 functions over 400 lines | none found | 0 |
| 4 components over 200 lines | none found | 0 |
| 5 dead code | proposed | 6 |
| 6 silent/empty catch | none found | 0 |
| 7 UI API calls missing loading/error | proposed | 1 |
| 8 DB queries in route handlers | none found | 0 |
| 9 sync I/O in request handlers | none found | 0 |
| 10 list endpoints without pagination | none found | 0 |
| 11 inconsistent API shapes | proposed | 3 |
| 12 floats for money | proposed | 7 |
| 13 dates as plain strings not ISO 8601 | proposed | 5 |
| 14 external calls without retry/backoff | proposed | 5 |
| 15 stale comments | proposed | 4 |
| 16 unvalidated input | proposed | 8 |
| 17 API routes missing auth | none found | 0 |
| 18 missing indexes | none found | 0 |
| 19 N+1 queries | none found | 0 |
| 20 SDKs initialized in more than one place | none found | 0 |

**Totals:** 8 checks proposed (39 findings). 12 checks none found.

This report is audit-only. No application code was changed.
